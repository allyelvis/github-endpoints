import 'package:flutter/material.dart';

class OrderingSystemModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ordering System Module'),
      ),
      body: Center(
        child: Text('Ordering System Module Content'),
      ),
    );
  }
}
