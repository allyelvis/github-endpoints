import 'package:flutter/material.dart';

class InvoiceModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Invoice Module'),
      ),
      body: Center(
        child: Text('Invoice Module Content'),
      ),
    );
  }
}
