import 'package:flutter/material.dart';

class POSModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('POS Module'),
      ),
      body: Center(
        child: Text('POS Module Content'),
      ),
    );
  }
}
