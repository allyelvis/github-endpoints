name: aenzbi_inventory_system
description: Aenzbi Inventory System with Invoice, Product Management, Ordering, Stock Management, POS, Accounting, and EBMS Integration.

publish_to: 'none'  # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^6.0.0
  sqflite: ^2.0.0+4
  path_provider: ^2.0.2
  json_annotation: ^4.0.1
  flutter_bloc: ^7.0.0
  equatable: ^2.0.0
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.7
  json_serializable: ^6.2.0

flutter:
  uses-material-design: true
